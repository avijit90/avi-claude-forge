{
  "description": "lgtm: stamp PreToolUse calls 'Looks Good To Me' (allow) unless they match a permissions.deny rule from settings.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/approve.js",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
